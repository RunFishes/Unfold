{
  "name": "sage",
  "version": "0.0.0",
  "description": "AI page understanding framework — semantic attributes + DOM dehydration",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "rm -rf dist-test && tsc -p tsconfig.test.json && node --test dist-test/tests/*.test.js",
    "typecheck": "tsc --noEmit"
  },
  "files": [
    "dist"
  ],
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}
