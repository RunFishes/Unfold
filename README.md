# sage

AI page understanding framework — semantic attributes + DOM dehydration.

Framework-agnostic browser library. Zero runtime dependencies.

## Status

Phase 0 — scaffolding only. No features yet.

## Scripts

```bash
npm install
npm run build      # tsc → dist/
npm run typecheck  # tsc --noEmit
npm test           # compile tests then node --test
```
